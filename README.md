
  # User profiles and service flow

  This is a code bundle for User profiles and service flow. The original project is available at https://www.figma.com/design/MRC3Wlut3caVcAsnvcBGYP/User-profiles-and-service-flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  